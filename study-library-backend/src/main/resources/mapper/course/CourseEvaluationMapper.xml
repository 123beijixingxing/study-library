<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studylib.modules.course.mapper.CourseEvaluationMapper">

  <resultMap id="CourseEvaluationResultMap" type="com.studylib.modules.course.entity.CourseEvaluationEntity">
    <id column="evaluation_id" property="evaluationId" />
    <result column="course_name" property="courseName" />
    <result column="username" property="username" />
    <result column="score" property="score" />
    <result column="content" property="content" />
    <result column="status" property="status" />
    <result column="create_time" property="createTime" />
    <result column="created_at" property="createdAt" />
    <result column="updated_at" property="updatedAt" />
    <result column="created_by" property="createdBy" />
    <result column="updated_by" property="updatedBy" />
    <result column="deleted" property="deleted" />
  </resultMap>

  <select id="selectById" resultMap="CourseEvaluationResultMap">
    select evaluation_id, course_name, username, score, content, status, create_time, created_at, updated_at, created_by, updated_by, deleted
    from course_evaluations
    where evaluation_id = #{evaluationId}
  </select>

  <select id="countPage" resultType="java.lang.Long">
    select count(1)
    from course_evaluations
    <where>
      <choose>
        <when test="status != null and status == 'deleted'">
          and deleted = true
        </when>
        <otherwise>
          and deleted = false
        </otherwise>
      </choose>
      <if test="keyword != null and keyword != ''">
        and (course_name like concat('%', #{keyword}, '%') or username like concat('%', #{keyword}, '%') or content like concat('%', #{keyword}, '%'))
      </if>
      <if test="status != null and status != '' and status != 'all' and status != 'deleted'">
        and status = #{status}
      </if>
      <if test="courseName != null and courseName != '' and courseName != 'all'">
        and course_name = #{courseName}
      </if>
    </where>
  </select>

  <select id="selectPage" resultMap="CourseEvaluationResultMap">
    select evaluation_id, course_name, username, score, content, status, create_time, created_at, updated_at, created_by, updated_by, deleted
    from course_evaluations
    <where>
      <choose>
        <when test="status != null and status == 'deleted'">
          and deleted = true
        </when>
        <otherwise>
          and deleted = false
        </otherwise>
      </choose>
      <if test="keyword != null and keyword != ''">
        and (course_name like concat('%', #{keyword}, '%') or username like concat('%', #{keyword}, '%') or content like concat('%', #{keyword}, '%'))
      </if>
      <if test="status != null and status != '' and status != 'all' and status != 'deleted'">
        and status = #{status}
      </if>
      <if test="courseName != null and courseName != '' and courseName != 'all'">
        and course_name = #{courseName}
      </if>
    </where>
    order by evaluation_id desc
    limit #{pageSize} offset #{offset}
  </select>

  <update id="updateStatus">
    update course_evaluations
    set status = #{status},
        deleted = #{deleted},
        updated_at = #{updatedAt},
        updated_by = #{updatedBy}
    where evaluation_id = #{evaluationId}
  </update>
</mapper>
