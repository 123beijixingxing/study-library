# 验收清单

## 1. 规范类验收

- `学习通OpenAPI草案.yaml` 可正常打开并被工具识别
- `学习通接口清单.md` 与 OpenAPI 草案路径一致
- `学习通接口DTO字段字典.md` 与 OpenAPI schema 命名一致
- `学习通前后端状态码规范.md` 与错误响应示例一致

## 2. 联调类验收

- Postman 基础集合可正常导入
- Postman 扩展集合可正常导入
- 环境变量文件可正常导入或手动配置
- 成功/失败示例报文可支撑联调说明

## 3. 样板类验收

- `代码模板示例/backend/` 目录完整
- `代码模板示例/frontend/` 目录完整
- Java 样板具备 `common/`、`modules/`、`resources/` 基础结构
- 前端样板具备 `api/`、`types/`、`constants/`、`config.ts` 基础结构

## 4. 测试类验收

- P1 冒烟 CSV 可直接导入测试平台
- 完整回归 CSV 可直接导入测试平台
- 状态流转说明与测试用例编号可相互对应

## 5. 交接类验收

- 新成员可依据 `操作手册.md` 找到所需资产
- 可依据 `asset-manifest.json` 追踪源文件位置
- 交付包目录命名清晰，便于归档和再次分发
